{
  "name": "typescript-app",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "ts": "tsc -w",
    "serve": "cd public && web-dev-server --node-resolve --watch --open --app-index playground.html",
    "dev": "yarn ts & yarn serve"
  },
  "devDependencies": {
    "@web/dev-server": "^0.1.18",
    "typescript": "^4.3.5"
  }
}
